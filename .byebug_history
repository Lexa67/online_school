c
@main_menu.each do |item, options| puts options end
@main_menu.each do |item, options| puts item puts options end
end
    puts options
    puts item
@main_menu.each do |item, options|
@main_menu = { teachers: { name: 'teacher', path: admin_teachers_path }, answers: { name: 'answer', path: admin_teachers_path } }
@main_menu = { teachers: { name: 'teacher', path: admin_teachers_path }, answers: { name: 'answer', path: admin_answer_path } }
end
    puts options
    puts item
@main_menu.each do |item, options|
end
    puts options
    puts item
@main_menu.each do |item, options|
    puts options
    puts item
@main_menu.each do |item, options|
@main@main_menu.each do
@main_menu.each do |item, options|
